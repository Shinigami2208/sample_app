validates:
  user:
    max_length_name: 50
    max_length_email: 255
    min_length_password: 10
    VALID_EMAIL_REGEX: !ruby/regexp '/\A[\w+\-.]+@[a-z\d\-]+(\.[a-z\d\-]+)*\.[a-z]+\z/i'
    image_gravatar_size: 100
